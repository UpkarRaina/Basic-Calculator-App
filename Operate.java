interface Operate {
    //  To define a common interface for different mathematical operations.
    Double getResult(Double... numbers);
    // Declares a method getResult that takes a variable number of Double arguments and returns a Double result.
}
